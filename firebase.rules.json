{
  "rules": {
    "orgs": {
      "$org": {
        ".read":
          "auth != null &&  (auth.token.email.replace('.', '_').endsWith($org) && auth.token.email_verified == true || auth.token.isAdmin == true)",
        ".write":
          "auth != null &&  (auth.token.email.replace('.', '_').endsWith($org) && auth.token.email_verified == true  || auth.token.isAdmin == true)",
        "groups": {
          "$group": {
            "userIds": {
              "$userId": {
                ".write": "$userId === auth.uid  || auth.token.isAdmin == true"
              }
            }
          }
        },
        "users": {
          "$userId": {
            ".write": "$userId === auth.uid  || auth.token.isAdmin == true",
            "groupIds": {
              ".read": "$userId === auth.uid  || auth.token.isAdmin == true"
            }
          }
        }
      }
    }
  }
}
